PalServer Enterprise (PalServer_ent)
PalServer_ent는 리눅스(Debian/Ubuntu) 환경에서 Docker를 기반으로 Palworld Dedicated Server를 구축, 운영, 모니터링하기 위한 올인원 엔터프라이즈 솔루션입니다. 강력한 CLI 도구(paladmin)와 직관적인 웹 대시보드를 통해 서버의 라이프사이클을 완벽하게 제어합니다.

🏗 System Architecture & Network
이 시스템은 보안을 위해 Public과 Private 두 개의 Docker 네트워크로 격리되어 운영됩니다.

Host System: Linux (Ubuntu 20.04+ / Debian)

Network Layer:

pal-public-net: 외부와 통신하는 Web UI 및 Proxy 컨테이너가 위치합니다.

pal-private-net (Internal): DB 및 백엔드 서비스 간의 통신을 담당하며 외부 접근이 차단됩니다.

Control Layer:

CLI: /usr/local/bin/paladmin (시스템 전역 명령어로 등록됨)

Web UI: React(Frontend) + Python(Backend) + Nginx

🛠 Installation Guide
설치 스크립트는 Docker 설치 여부 확인부터 네트워크 구성, 웹 UI 배포까지 자동으로 수행합니다.

1. Prerequisites (사전 요구사항)
OS: Debian 또는 Ubuntu 계열 리눅스 (필수)

Account: sudo 권한이 있는 사용자

Ports: 8443 (Web UI), 8211 (Game UDP) 오픈 필요

2. Setup Steps
Repository Clone

Bash

git clone <repository-url>
cd PalServer_ent
Run Installer 설치 스크립트에 실행 권한을 부여하고 실행합니다.

Bash

chmod +x install.sh
./install.sh
Installation Process (Interactive) 스크립트 실행 중 다음과 같은 작업이 진행됩니다:

Dependency Check: Docker 및 Docker Compose가 없으면 자동으로 설치합니다.

CLI Setup: paladmin 명령어를 시스템 전역(/usr/local/bin)에 등록합니다.

Image Build (Optional): "Do you want to build Palworld server image now?" 질문에 y를 입력하면 커스텀 서버 이미지를 빌드합니다.

Network Init: pal-public-net, pal-private-net 네트워크를 생성합니다.

Web UI Start: 관리자 대시보드 컨테이너를 구동합니다.

Completion (Optional): 명령어 자동완성 기능을 등록합니다.

Verification 설치가 완료되면 아래와 같은 정보가 출력됩니다.

Plaintext

[SUCCESS] Installation completed successfully.
- Admin CLI : paladmin
- Web UI    : http://<server-ip>:8443
💻 Usage
1. Web Dashboard
브라우저를 열고 http://<YOUR_SERVER_IP>:8443으로 접속합니다.

Dashboard: 서버 상태(CPU/RAM), 플레이어 접속 현황 확인.

Instance Mng: 서버 인스턴스 생성, 시작, 중지.

Settings: PalWorldSettings.ini 웹 에디터 제공.

2. CLI Tool (paladmin)
설치 시 등록된 paladmin 명령어를 터미널에서 직접 사용할 수 있습니다.

Bash

# 사용 가능한 명령어 확인
paladmin --help

# 주요 명령어 (예시)
paladmin start <instance_name>   # 특정 인스턴스 시작
paladmin backup <instance_name>  # 수동 백업 실행
paladmin update                  # 서버 업데이트 수행
🗑 Uninstallation
시스템을 제거해야 할 경우 uninstall.sh를 사용합니다. 데이터 보존 정책에 따라 안전하게 삭제됩니다.

Bash

./uninstall.sh
삭제되는 항목:

Web UI 관련 컨테이너 및 이미지 (Frontend, Backend, Nginx).

paladmin CLI 실행 파일.

Docker Networks (pal-public-net, pal-private-net).

보존되는 항목 (Safe):

server/instances/ 하위의 게임 세이브 데이터 및 설정 파일은 삭제되지 않습니다.

Palworld 서버 Docker 이미지는 삭제 여부를 선택할 수 있습니다 ("Remove paladmin-related docker images?").

📂 Directory Structure
Plaintext

PalServer_ent/
├── cmm/                  # Core Management Modules
│   ├── bin/paladmin.sh   # CLI 실행 스크립트 (Source)
│   ├── controllers/      # 백업, 업데이트, 롤백 로직 스크립트
│   └── make-pal-images/  # 커스텀 Docker 이미지 빌더
├── UI/                   # Web Interface
│   ├── backend/          # Python API Server
│   ├── frontend-admin/   # React Admin Dashboard
│   └── docker-compose.yml# UI 컨테이너 오케스트레이션
├── server/
│   ├── instances/        # [중요] 게임 데이터 저장소 (삭제 안됨)
│   └── backup/           # 백업 파일 저장소
├── install.sh            # 메인 설치 스크립트
└── uninstall.sh          # 메인 삭제 스크립트